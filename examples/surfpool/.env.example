# Stripe API Key (for sandbox mode)
# Get your key from: https://dashboard.stripe.com/test/apikeys
STRIPE_SANDBOX_SECRET_KEY=sk_test_your_key_here

# Solana Private Key (base58 encoded) for X402 payments
# This is required for real payment signatures and on-chain settlement.
# Without this key, the example will use mock payments which will fail verification.
#
# Generate a new keypair with: solana-keygen new --no-bip39-passphrase
# Then copy the private key (base58 string) from the generated file
PAYER_PRIVATE_KEY=your_base58_private_key_here
