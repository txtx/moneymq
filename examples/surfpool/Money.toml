# MoneyMQ Manifest
# Generated by 'moneymq init'

[project]
name = "my-project"
version = "0.1.0"

[providers.stripe]
provider_type = "stripe"
sandbox = "stripe_sandbox"

[providers.stripe_sandbox]
provider_type = "stripe"
test_mode = true